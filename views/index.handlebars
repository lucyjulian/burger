<h1>BURGERS</h1>

<h2>BURGERS YOU CAN EAT!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
        <li>
	        {{burger_name}}

            <button class="change-devoured" data-id="{{id}}">DEVOUR IT!</button>
	
        </li>
    {{/unless}}
  {{/each}}
</ul>

<h2>BURGERS SOMEONE ATE!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      <li>
          {{burger_name}}
      </li>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a BURGER</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bg">Burger Name:</label>
    <input type="text" id="bg" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">Already devoured?</label><br>
    <input type="radio" name="devoured" value="0" checked> No WAY!<br>
    <input type="radio" name="devoured" value="1"> Yes I ate it :D and it was delicious!
  </div>

  <button type="submit">Add Burger</button>
</form>
